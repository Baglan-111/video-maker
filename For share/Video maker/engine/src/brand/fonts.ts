import { loadFont } from '@remotion/google-fonts/Inter';

export const { fontFamily } = loadFont('normal', {
  subsets: ['cyrillic', 'latin'],
  // 600 и 800 добавлены для story@1: там иерархия строится размером и весом, а
  // не цветом (цветовая иерархия пропадает в ч/б и у людей с нарушением
  // цветовосприятия), и двух весов на пять ступеней шкалы не хватает.
  // Существующим шаблонам это ничего не меняет — они просят 400 и 700.
  weights: ['400', '600', '700', '800'],
});
